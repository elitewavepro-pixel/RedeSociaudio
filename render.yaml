services:
  - type: web
    name: redesociaudio
    runtime: python
    plan: free
    buildCommand: echo "Nenhuma dependência externa"
    startCommand: python server.py
    healthCheckPath: /api/health
